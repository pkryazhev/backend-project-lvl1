name: GitHub Actions Demo
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: npm install
        run: |
          make install
      - name: Run linter
        run: |
          make lint
